This is a minimal project structure and build scripting for creating Angular-based Electron applications.


To get started:
```
npm i // To install node dependencies
npm run start // To build and run the skeleton app.
```